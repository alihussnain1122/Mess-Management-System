@page
@model MessManagement.Areas.Admin.Pages.Payments.HistoryModel
@{
    ViewData["Title"] = "Payment History";
}

<div class="mb-6 flex justify-between items-center">
    <div>
        <h1 class="text-2xl font-bold text-white">Payment History</h1>
        <p class="text-white/60">View payment records by date range</p>
    </div>
    <a href="/Admin/Payments/Add" class="bg-gradient-to-r from-emerald-500 to-teal-500 hover:from-emerald-600 hover:to-teal-600 text-white px-4 py-2 rounded-xl flex items-center transition-all shadow-lg">
        <i class="fas fa-plus mr-2"></i>
        Add Payment
    </a>
</div>

<div class="bg-white/15 backdrop-blur-xl rounded-2xl border border-white/20 p-4 mb-6">
    <form method="get" class="flex flex-wrap items-end gap-4">
        <div>
            <label class="block text-sm text-emerald-400 mb-1">Start Date</label>
            <input type="date" name="startDate" value="@Model.StartDate.ToString("yyyy-MM-dd")" class="bg-white/10 border border-white/20 rounded-xl px-3 py-2 text-white focus:outline-none focus:ring-2 focus:ring-emerald-500" />
        </div>
        <div>
            <label class="block text-sm text-emerald-400 mb-1">End Date</label>
            <input type="date" name="endDate" value="@Model.EndDate.ToString("yyyy-MM-dd")" class="bg-white/10 border border-white/20 rounded-xl px-3 py-2 text-white focus:outline-none focus:ring-2 focus:ring-emerald-500" />
        </div>
        <button type="submit" class="bg-gradient-to-r from-emerald-500 to-teal-500 hover:from-emerald-600 hover:to-teal-600 text-white px-4 py-2 rounded-xl transition-all shadow-lg">Filter</button>
    </form>
</div>

<div class="bg-white/15 backdrop-blur-xl rounded-2xl border border-white/20 overflow-hidden">
    <table class="w-full">
        <thead class="bg-white/10 border-b border-white/20">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Date</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Member</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Amount</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Mode</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Status</th>
            </tr>
        </thead>
        <tbody class="divide-y divide-white/10">
            @if (Model.Payments != null && Model.Payments.Any())
            {
                @foreach (var payment in Model.Payments)
                {
                    <tr class="hover:bg-white/5">
                        <td class="px-6 py-4 text-white">@payment.Date.ToString("dd MMM yyyy")</td>
                        <td class="px-6 py-4 font-medium text-white">@payment.Member?.FullName</td>
                        <td class="px-6 py-4 font-semibold text-emerald-400">Rs.@payment.Amount.ToString("N2")</td>
                        <td class="px-6 py-4">
                            <span class="px-2 py-1 text-xs font-medium @(payment.PaymentMode == MessManagement.Models.PaymentMode.Cash ? "bg-blue-500/20 text-blue-400 border border-blue-500/30" : "bg-purple-500/20 text-purple-400 border border-purple-500/30") rounded-full">
                                @payment.PaymentMode
                            </span>
                        </td>
                        <td class="px-6 py-4">
                            <span class="px-2 py-1 text-xs font-medium @(payment.Status == MessManagement.Models.PaymentStatus.Completed ? "bg-emerald-500/20 text-emerald-400 border border-emerald-500/30" : "bg-yellow-500/20 text-yellow-400 border border-yellow-500/30") rounded-full">
                                @payment.Status
                            </span>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5" class="px-6 py-8 text-center text-white/50">No payments found</td>
                </tr>
            }
        </tbody>
    </table>
</div>