@page
@model MessManagement.Areas.Admin.Pages.Attendance.HistoryModel
@{
    ViewData["Title"] = "Attendance History";
}

<div class="mb-6 flex justify-between items-center">
    <div>
        <h1 class="text-2xl font-bold text-white">Attendance History</h1>
        <p class="text-white/60">View attendance records</p>
    </div>
    <a href="/Admin/Attendance/Mark" class="bg-gradient-to-r from-emerald-500 to-teal-500 hover:from-emerald-600 hover:to-teal-600 text-white px-4 py-2 rounded-xl flex items-center transition-all shadow-lg">
        <i class="fas fa-calendar-check mr-2"></i>
        Mark Attendance
    </a>
</div>

<div class="bg-white/15 backdrop-blur-xl rounded-2xl border border-white/20 p-4 mb-6">
    <form method="get" class="flex flex-wrap items-end gap-4">
        <div>
            <label class="block text-sm text-emerald-400 mb-1">Start Date</label>
            <input type="date" name="startDate" value="@Model.StartDate.ToString("yyyy-MM-dd")" class="bg-white/10 border border-white/20 rounded-xl px-3 py-2 text-white focus:outline-none focus:ring-2 focus:ring-emerald-500" />
        </div>
        <div>
            <label class="block text-sm text-emerald-400 mb-1">End Date</label>
            <input type="date" name="endDate" value="@Model.EndDate.ToString("yyyy-MM-dd")" class="bg-white/10 border border-white/20 rounded-xl px-3 py-2 text-white focus:outline-none focus:ring-2 focus:ring-emerald-500" />
        </div>
        <button type="submit" class="bg-gradient-to-r from-emerald-500 to-teal-500 hover:from-emerald-600 hover:to-teal-600 text-white px-4 py-2 rounded-xl transition-all shadow-lg">Filter</button>
    </form>
</div>

<div class="bg-white/15 backdrop-blur-xl rounded-2xl border border-white/20 overflow-hidden">
    <table class="w-full">
        <thead class="bg-white/10 border-b border-white/20">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Date</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Member</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Status</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-emerald-400 uppercase">Marked By</th>
            </tr>
        </thead>
        <tbody class="divide-y divide-white/10">
            @if (Model.AttendanceRecords != null && Model.AttendanceRecords.Any())
            {
                @foreach (var record in Model.AttendanceRecords)
                {
                    <tr class="hover:bg-white/5">
                        <td class="px-6 py-4 text-white">@record.Date.ToString("dd MMM yyyy")</td>
                        <td class="px-6 py-4 font-medium text-white">@record.Member?.FullName</td>
                        <td class="px-6 py-4">
                            <span class="px-2 py-1 text-xs font-medium @(record.Status == MessManagement.Models.AttendanceStatus.Present ? "bg-emerald-500/20 text-emerald-400 border border-emerald-500/30" : "bg-red-500/20 text-red-400 border border-red-500/30") rounded-full">
                                @record.Status
                            </span>
                        </td>
                        <td class="px-6 py-4 text-white/70">@record.MarkedByUser?.Username</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="px-6 py-8 text-center text-white/50">No records found</td>
                </tr>
            }
        </tbody>
    </table>
</div>